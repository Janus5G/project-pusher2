# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Install

```bash
python -m venv .venv
python -m pip install --upgrade pip
# Install from requirements.txt or pyproject.toml as appropriate.
```

## Test

```bash
python -m pytest
```

## Security

Never commit live tokens, credentials, private keys, or populated `.env` files.
